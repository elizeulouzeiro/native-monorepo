import React from 'react';
import {View, Text} from 'react-native';

const Home = () => (
  <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
    <Text style={{fontSize: 32}}>Área Logada</Text>
  </View>
);

export default Home;
